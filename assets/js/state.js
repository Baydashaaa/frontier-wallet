// State that more than one module both reads and writes. Kept in one
// object because an imported binding cannot be assigned to.
export const S = {
  MNEMONIC: null,
  PASS: null,
  BITS: 128,
  SLOTS: [],
  SEL: null,
  SAVED: null,
};
